services:
  - type: web
    name: imwallet-proxy
    runtime: node
    plan: free
    buildCommand: "echo 'No build needed'"
    startCommand: "node server.js"
    envVars:
      - key: PROXY_SECRET
        value: biq_imw_proxy_2026
      - key: NODE_ENV
        value: production
